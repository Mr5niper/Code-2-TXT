# Build-time requirements for Code-2-TXT.
#
# The app itself uses only the Python standard library (tkinter, pathlib,
# re, etc.), so there are no runtime dependencies. These are only needed
# to build the .exe with PyInstaller. Versions match a known-good build
# on Python 3.13.12.
pyinstaller==6.16.0
pyinstaller-hooks-contrib==2025.9
